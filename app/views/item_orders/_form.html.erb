<%= form_for(@item_order) do |f| %>
  <% if @item_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item_order.errors.count, "error") %> prohibited this item_order from being saved:</h2>

      <ul>
      <% @item_order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :item_id %><br />
    <%= f.number_field :item_id %>
  </div>
  <div class="field">
    <%= f.label :order_id %><br />
    <%= f.number_field :order_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
