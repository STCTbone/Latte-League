
<br>
<p id="notice"><%= notice %></p>

<% content_for :breadcrumb do %>
  <li class="active"> Home / items</li>
<% end %>

<h1>Item Details</h1>

<div class="well">

  <%= link_to 'Show all items', items_path, :class => 'btn btn-primary' %>

</div>

<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Price</th>
      <th>Country</th>
      <th>City</th>
      <th>Category</th>
      <th>Is available?</th>


      <th>&nbsp;</th>
    </tr>
  </thead>
  <tbody>
      <tr>
        <td><%= @item.price %></th>        
        <td><%= @item.price %></th>        
        <td><%= @item.country %></th>        
        <td><%= @item.city %></th>        
        <td><%= @item.category_id %></th>        
        <td><%= @item.is_available %></th>        
        
    
        <td>
  <%= link_to 'Edit', edit_item_path(@item), :class => 'btn btn-primary' %>
  <%= link_to 'Delete', @item, :class => 'btn btn-danger', :confirm => 'Are you sure?', :method => :delete  %>
        </td>
      </tr>
  </tbody>
</table>
<br>